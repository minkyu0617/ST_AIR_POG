# CLAUDE.md

이 저장소는 Steam 출시를 목표로 하는 아케이드 비행 게임 프로젝트입니다.

| 구분 | 값 |
|---|---|
| 저장소 | `ST_AIR_POG` |
| **프로젝트 코드명** | **`FPG`** 🔒 영구 고정 |
| **게임 제목** | **미정** (M4 전 확정 예정) |

⚠️ **게임 제목을 코드·에셋 경로·데이터 ID에 넣지 마십시오.** 제목은 문자열 테이블과 스토어 자산에만 존재합니다. 제목이 바뀌어도 코드는 손대지 않습니다.

## 작업 시작 전 반드시 읽을 것

👉 **[`FPG.md`](FPG.md)** — 프로젝트 인수인계 문서. 현재 상태, 확정/미결정 사항, 다음 할 일, 설계 원칙이 전부 여기 있습니다.

## 현재 단계

**프리프로덕션 완료, 코드 0줄.** 설계 문서만 존재합니다 (`docs/` 18종).
다음은 M0(설계 확정) → M1(수직 슬라이스).

## 작업 규칙

1. **결정을 내렸으면 `docs/14_open_questions.md`에 기록**하십시오. 결정 기록이 없으면 다음 세션에서 같은 논의를 반복합니다.
2. **`FPG.md`의 §1(현재 상태)과 §5(다음 할 일)를 갱신**하십시오. 이 파일이 세션 간 유일한 연결점입니다.
3. **설계 원칙(`FPG.md` §3)을 어기는 제안을 하지 마십시오.** 특히:
   - 자원(HP·탄약·크레딧·순위)은 클라이언트에서 예측하지 않습니다
   - 밸런스 수치는 코드에 하드코딩하지 않습니다 (CSV로)
   - P2W 요소를 넣지 않습니다 (기체 스탯 총합 100 고정)
4. **상표·세무·게임물 등급은 외부 확인 사항**입니다. 문서 내용을 근거로 단정하지 마십시오.

## 이름 규칙

| 대상 | 규칙 | 예 |
|---|---|---|
| 클래스 접두어 | `FPG` | `AFPGAircraftPawn`, `UFPGDataRegistry`, `FFPGMove` |
| 모듈 / 소스 폴더 | `FPGGame` | `Source/FPGGame/` |
| 데이터 ID | 대문자 스네이크 | `AIRCRAFT_FALCON`, `ITEM_RAILGUN` |

## 문서 작성 규칙

- 문서는 한국어로 작성합니다.
- 수치를 제시할 때는 **근거나 출처 문서를 함께** 답니다.
- 불확실한 것은 불확실하다고 명시합니다. 특히 Steam 정책은 수시로 바뀝니다.
